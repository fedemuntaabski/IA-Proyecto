# Dependencias principales para Pictionary Live
# Python 3.10, 3.11, 3.12
# Versiones específicas compatibles para evitar conflictos

# Procesamiento de imágenes y video
numpy>=1.24.0
opencv-python>=4.5.0

# Detección de manos (CRÍTICO para la aplicación)
# MediaPipe requiere protobuf compatible
mediapipe>=0.10.0

# Inferencia de modelos (opcional para modo demo)
# TensorFlow versión específica compatible
tensorflow-cpu==2.18.0

# Protobuf - versión específica compatible con TensorFlow 2.18.0 y MediaPipe
protobuf==4.25.3

# Utilidades
ndjson>=0.3.0

# NOTAS DE INSTALACIÓN:
# 1. Usar Python 3.10, 3.11 o 3.12
# 2. En Windows, podría necesitar Visual C++ redistributable
# 3. El archivo main.py instala dependencias automáticamente
# 4. MediaPipe está optimizado para CPU (mejor performance que GPU en este caso)
# 5. Versiones específicas para evitar conflictos con protobuf (4.25.3 funciona con TF 2.18.0 y MediaPipe)

